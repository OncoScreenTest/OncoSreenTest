{
  "files": [], // нет отдельных файлов, используются только ссылки ниже
  "references": [
    // ссылаемся на две отдельные конфигурации TypeScript
    { "path": "./tsconfig.app.json" }, // конфигурация для приложения
    { "path": "./tsconfig.node.json" } // конфигурация для конфига Vite
  ]
}
